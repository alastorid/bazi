# 命盤評分模型

本專案把紫微斗數評分視為可查核的傳統命理研究工具，不是科學、醫療、財務或人生結果預測。

## 三層評分

`src/scoring-model.mjs` 是唯一版本控制來源，依序處理：

1. 白名單星曜落宮：例如祿存、武曲、貪狼在財帛；文昌、文曲在命宮。
2. 四化落宮：例如化權入官祿、化忌入財帛。
3. 複合格局：例如紫府坐垣、七殺朝斗、巨日格、權祿相逢命、祿馬交馳。格局可以同時作用多個維度，權重遠高於單星。

評分維度為幸運、財富、經商、社交、事業、官運、專業、科甲、才藝、外貌、魅力、桃花、婚姻與綜合。原始分保留負數與超過 100 的值，不截斷；SSS～F 由當年全部命盤的百分位決定。

## 亮度

`src/ziwei-algorithm.mjs` 直接保存固定版本 `iztro` 回傳的亮度。查詢順序為 `陷 < 不 < 平 < 利 < 得 < 旺 < 廟`。亮度不是統一加減分，而是依星性、規則正負與落宮，調節該規則本身的作用；煞星廟旺不會被直接解釋成吉。

## 星曜白名單

`src/explained-stars.mjs` 是唯一白名單來源，只含三份指定教材有明確解釋的 36 顆星曜。四化另列為轉化物件，不算普通星曜。排盤引擎產生但教材未解釋的名稱，不建立寬表欄位，也不進入各宮 `全部星`、`星曜亮度`、評分、預設查詢或自動完成。`地空` 可查詢，但只在教材明示的宮位／組合規則中作用，不因單獨出現取得通用分數。

## 資料表

- `命盤`：原始寬表，36 顆白名單星曜各有星等與宮位，每宮各有主星、經過白名單過濾的全部星與大限。
- `星曜定義`：36 顆星的層級、證據層級、核心含義與使用限制。
- `星曜亮度`、`亮度等級`：可直接排序與查詢的正規化亮度資料。
- `格局規則`：格局／組合目錄，另存類型、嚴格度、結構稀有度、完整解釋、可計算與可計分狀態。教材只列名稱但未完整解釋的夾類也保留在此，但不得產生旗標或分數。
- `關係格局規則`：母子格等兩人命盤對應法，與單張命盤排名完全分離。
- `格局規則作用`：一個格局對各維度及適用性別的獨立作用列。
- `命盤格局`：每張命盤的格局旗標。
- `評分規則`、`命盤評分明細`：實際命中規則、亮度倍率、基礎作用與實際貢獻。
- `命盤評分`：各維度的原始分、年度百分位與排名。

## 公式

```text
維度原始分 = Σ(單星落宮作用 × 星性／正負／亮度倍率)
             + Σ(四化落宮作用 × 星性／正負／亮度倍率)
             + Σ(已成立複合格局的多維度作用)

綜合分 = 所有維度原始分總和
排名 = 原始分在該生成年度全部命盤中的百分位級別
```

產生器、評分明細、格局旗標、查詢庫與視覺化只讀同一份預計算結果。`scripts/verify-data.mjs` 會由 SQL 獨立重算所有可計算格局旗標、確認未完整解釋的格名不可計算／不可計分、核對每個分數與明細總和，並找出同星同宮但亮度不同的命盤作回歸比較。
