# 命盤評分

`命盤評分` 是由全年 `命盤` 原始資料產生的獨立排名表。它屬於傳統紫微斗數的結構化傾向評分，不是科學預測；其中 `福體` 只代表命理上的穩定、韌性與生活順遂傾向，不是醫療診斷或健康預測。

## 資料架構

```text
命盤（raw，KEY 主鍵）
├── 評分規則（加分／組合／扣分條件與權重）
├── 評分維度（基礎分與綜合權重）
└── 排名門檻（全年百分位門檻）
         ↓
命盤評分（KEY 外鍵，9 維 × 分數／排名／百分位）
         ↓
命盤完整評分（raw + scores 的唯讀 view）
```

`命盤` 不加入任何評分欄位。修改規則後可重建 `命盤評分`，不需要改動原始命盤 schema。

## Schema

`命盤評分` 為一個 KEY 對一筆評分，包含：

- 財富、橫財、幸運、外貌、事業、社交、家庭助力、福體、綜合。
- 每個維度分別存 `分`、`排名`、`百分位`，共 27 個評分欄位。
- 分數為 0–100；百分位為該命盤相對全年 8,760 筆資料的位置。

規則設定表：

- `評分規則`：規則 ID、維度、類型、權重、條件 SQL、說明。
- `評分維度`：每個維度的基礎分與綜合權重。
- `排名門檻`：排名和最低百分位。
- `命盤完整評分`：把完整 raw 命盤與全部排名欄位連接起來的查詢 view。

## 初始權重

| 維度 | 綜合權重 | 主要結構 |
|---|---:|---|
| 財富 | 20% | 財帛、官祿、田宅、命宮；財星、祿權、守財能力與破壞因素 |
| 橫財 | 0% | 火貪／鈴貪、殺破狼、祿權爆發；獨立觀察且不重複灌入綜合分 |
| 事業 | 17% | 官祿、命宮、財帛、遷移；領導、執行、專業認可與開創格局 |
| 福體 | 15% | 福德、命宮、疾厄；穩定、支持、韌性與破壞因素 |
| 幸運 | 14% | 命宮、福德、遷移、父母；吉化、貴人與跨宮支持 |
| 家庭助力 | 14% | 父母、田宅、福德、命宮；家庭資源、支持與資產累積 |
| 外貌 | 10% | 命宮、身宮；多種外貌風格、昌曲桃花與和諧度 |
| 社交 | 10% | 命宮、僕役、遷移、夫妻；人緣、網絡、助力與衝突傾向 |

核心七維由基礎分 50 開始，橫財由 35 開始。76 條規則分成一般加分、跨星／跨宮組合加分與扣分；最終分數限制在 0–100。相同星曜在不同宮位使用不同權重，核心宮位的化忌、空劫及主要煞曜會降低相應維度。

綜合分使用七維加權平均。所有維度再以 SQLite `PERCENT_RANK()` 對全年資料計算百分位，因此原始分接近的命盤仍可進行相對比較。

## 財富與橫財分開

`財富` 表示收入、累積、守成與資產穩定；`橫財` 表示突然放大的機會，兩者不是同一件事。橫財的綜合權重為 0，因此可以單獨排序，又不會把爆發訊號重複計入綜合分。

火貪與鈴貪採嚴格同宮判定：`貪狼宮位 = '財帛'`，而且火星／鈴星必須出現在 `財帛全部星`。火星或鈴星落在其他宮位不會得到這項 synergy bonus。廟旺、化祿、祿存會再加分；空劫或財帛化忌加煞則扣分。

## 排名稀有度

| 排名 | 最低百分位 | 約略範圍 |
|---|---:|---:|
| SSS | 99 | 前 1% |
| SSR | 96 | 接續 3% |
| SS | 90 | 接續 6% |
| S | 80 | 接續 10% |
| A | 60 | 接續 20% |
| B | 35 | 接續 25% |
| C | 20 | 接續 15% |
| D | 10 | 接續 10% |
| E | 4 | 接續 6% |
| F | 0 | 後 4% |

同分資料共享百分位，所以實際數量可能因同分而略有差異。驗證程序會阻止任一維度的 SSS 膨脹到超過全體 3%。

## 調整方式

規則集中在 `src/scoring-model.mjs`，每條規則都有維度、類型、權重、SQL 條件及說明。調整單一權重或新增組合規則後重新執行年度 build 即可；前端只查詢生成後的資料表，不包含評分邏輯。

## Sample query migration

範例資料改為 `{ key, group, label, description, rankTarget, sql }`，命名統一為 `category_purpose`。舊的 `sr_income`、`sr_windfall`、`fire_greed`、`s_parents` 等名稱已由 `wealth_income`、`windfall_sss`、`windfall_fire_greed`、`family_parents` 取代。

以前每個 SQL 都重新寫一遍星曜、四化、廟旺與吉煞邏輯；現在先用排名回答廣泛品質問題，只在火貪等具名格局保留少量 raw 條件。每個會回傳命盤列的範例，前 11 欄固定為 KEY／連結／性別，以及綜合、財富、幸運、外貌的排名和分數。

```sql
SELECT m."KEY", m."命盤連結", r.*
FROM "命盤評分" r
JOIN "命盤" m ON m."KEY" = r."KEY"
WHERE r."財富排名" IN ('SSS', 'SSR')
ORDER BY r."綜合分" DESC;
```
