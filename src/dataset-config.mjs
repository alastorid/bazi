// These historically significant dates are always included in addition to the
// complete year or year range passed to the build script. Dates already covered
// by that range are de-duplicated by the generator.
export const SUPPLEMENTAL_DATES = Object.freeze([
  "1946-06-14",
  "1991-12-19",
]);
