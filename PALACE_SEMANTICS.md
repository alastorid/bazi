# 空宮、對宮與真宮

`命盤` 保留所有原始星曜與宮位資料，並加入可直接查詢的空宮語義。這些欄位是結構化的傳統紫微斗數資料，不是分數或排名。

## 五層資料

1. **原宮**：星曜實際被排入的宮位。原有的 `命宮主星`、`財帛主星`、`財帛全部星` 等欄位不變。
2. **是否空宮**：只有該宮沒有紫微十四主星時才是 `1`。輔星、吉曜、煞曜或四化不能把空宮變成非空宮。
3. **對宮**：固定的相對宮位，並直接提供對宮主星與全部星。
4. **真宮**：若原宮非空，使用本宮；若原宮空宮，借用對宮。`真X宮` 與 `真X宮主星` 都是有效主星，`真X宮全部星` 是採用來源宮位的完整星曜字串。
5. **來源**：`真X宮來源` 明確記錄 `本宮` 或 `借對宮`，不隱藏 provenance。

例如父母宮空宮時：

```text
父母主星          = ''
父母宮是否空宮    = 1
父母宮對宮        = 疾厄宮
父母宮對宮主星    = 天府
真父母宮主星      = 天府
真父母宮來源      = 借對宮
```

## 對宮映射

| 原宮 | 對宮 |
|---|---|
| 命宮 | 遷移宮 |
| 兄弟宮 | 交友宮 |
| 夫妻宮 | 官祿宮 |
| 子女宮 | 田宅宮 |
| 財帛宮 | 福德宮 |
| 疾厄宮 | 父母宮 |

反方向使用相同配對。iztro 原始欄位中的 `僕役` 對外以傳統的 `交友宮` 語義名稱呈現，但原始 `僕役主星`、`僕役全部星` 等欄位仍完整保留。

## 每宮新增欄位

十二宮各自新增：

```text
X宮地支
X宮是否空宮
X宮對宮
X宮對宮主星
X宮對宮全部星
真X宮
真X宮主星
真X宮全部星
真X宮來源
```

另有 `空宮數`，等於十二個 `是否空宮` 欄位之和。

## 嚴格格局與借對宮

需要實際同宮或實際落宮的具名格局，只能查原始欄位。例如倪師定義的火貴格必須同時滿足：

```sql
WHERE "貪狼宮位" = '命宮'
  AND "火星宮位" = '命宮'
  AND "命宮主星" LIKE '%貪狼%'
  AND "命宮全部星" LIKE '%火星%'
```

不能以 `真命宮主星 LIKE '%貪狼%'` 代替，否則「命宮空宮借入貪狼＋本宮火星」會被錯認成火貪同宮。

較廣泛的宮位性質研究可以使用真宮，但查詢結果應同時顯示原宮、是否空宮與來源，讓使用者知道該星來自本宮或對宮。
