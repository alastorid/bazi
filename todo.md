# bazi 待辦與驗收

## 目前目標
2026—2035 全年度，加上 1946-06-14 與 1991-12-19。預期 87,696 張命盤。
正式資料只在 GitHub Actions 產生；本地僅做程式與小型回歸測試。

## 已實作，等待十年正式資料驗收
- [x] 十年度 matrix 平行生成、年度快取、合併時指定日期去重。
- [x] DuckDB 命盤排名；同分並列，總序只作穩定顯示。
- [x] 全域、分年、分月、分性別、年×性別的頂端／底端診斷。
- [x] 十二宮權重各 ±10% 的全樣本敏感度；排序相關、頂端重合、最大名次變動。
- [x] 五張參考盤的全樣本穩定性驗證。
- [x] 排名分佈、宮位統計、頂端特徵及頂端／底端各五張快照。
- [x] 命盤比較星曜、命盤比較宮位、命盤對比明細分表保存依據。
- [x] 篩選結果轉子集排名 SQL；月份範例可編輯參數，同分共享名次。
- [x] 最外層「命盤比較」，甲乙選盤、交換、逐宮逐星對照。
- [x] 時間軸保留吉凶格模式，新增排名百分位及可點選的月統計／峰值。
- [x] 比較器單元測試及 240 筆人工合成資料的原生 DuckDB SQL 回歸。
- [ ] Actions 完成十年合併，核對 87,696 筆與至少 10,000 筆驗證門檻。
- [ ] 真實資料瀏覽器測試與桌面／手機截圖驗收。
- [ ] 記錄實際壓縮大小、診斷與敏感度結果，完成 Pages 部署。

## 重要修正：原接手備忘錄的不準確敘述
1. 比較器是逐宮作用差的加權模型。數學上可以寫成作用(A)−作用(B)，不是非加總演算法，也不是一般非傳遞的淘汰賽。因此參考盤只平移差值，不會改變排序；不需假設一般錦標賽具有傳遞性。
2. 「星曜亮度」表只保存有亮度的星，不能用它重建所有星。現在從 36 星白名單的原始宮位欄重建，未知亮度保留未知，沒有亮度不等於不存在。
3. 百分位越高越前：百分位＝100×[1−(排名序−1)/(N−1)]；單筆為 100。PR 為百分位／100。相同作用共用名次，不能用唯一總序拆散同分者。
4. 統計只能查模型訊號方向，不能證明真實人生好壞，也不能保證每張高百分位命盤無凶格。亮度等診斷與模型輸入重疊，不應宣稱全部是獨立驗證。
5. 固定星曜數值與宮位權重是工程模型參數，不是教材提供的定量定律。煞星廟旺不等於更吉；地空不加通用單星分。
6. 格局規則與比較排名分開保存；原有年度評分相容表仍是各年度計算，不可誤認成新比較器的全域百分位。
7. 不再沿用「97% idle」或巨大資料唯一解法的未驗證斷言。十年採年度平行生成；瀏覽器仍載入一份合併 DuckDB，時間軸按需繪製與查詢。更大範圍需另測記憶體或採遠端分片讀取。

## 資料流與驗證
Actions：plan → 十年度 generate → merge → 全域排名／診斷 → 原生 SQL → Pages 打包 → 真實 DuckDB-WASM 瀏覽器驗證 → 部署。

執行入口（供 runner）：
```sh
npm run build -- 2026-2035
MIN_SAMPLE=10000 node scripts/verify-ranking.mjs
node scripts/verify-queries.mjs
node scripts/verify-browser.mjs
```

本地小型回歸：
```sh
node scripts/verify-comparator.mjs
uv run --with duckdb==1.3.2 python scripts/verify-native-fixture.py
```

模型：src/bazi-comparator.mjs。
逐星資料生成：scripts/rank-bazi.mjs。
全域排名及診斷：scripts/ranking_native.py。
年分合併：scripts/merge-shards.py。
UI：browser.js、terminal.js、visualization.js、comparison.js。
